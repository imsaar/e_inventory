# Environment Configuration Example
# Copy this file to .env and adjust values as needed

# Node Environment
NODE_ENV=development

# Database Configuration
# Leave empty to use default paths based on NODE_ENV
DB_PATH=
DATA_DIR=

# Server Configuration
PORT=3001

# Development vs Production Database Paths
# Development: data/inventory-dev.db
# Production: data/inventory.db  
# Test: data/test/test-inventory-{timestamp}.db

# Production Database Directory (optional)
# Only used in production if DATA_DIR is set
# DATA_DIR=/var/lib/inventory-app/data

# Custom Database Path (optional)
# Overrides all other database path logic
# DB_PATH=/path/to/custom/inventory.db